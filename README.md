# 衍生式產品設計（進修部）課程講義

網站：https://evia918.github.io/1151-evening-merch/

- 根目錄的 `*.html` 為發布檔，由 `_src/build.py` 產生，請勿直接修改。
- 修改內容：編輯 `_src/pages/*.html` 或 `_src/style.css`，再執行 `python _src/build.py`。
- 本 repo 為公開網站，只放學生講義；教案、名單等教師端資料不得上傳。
